
	  _______________
	 |               |
	 |               |
	 |    libemu     |
	 | x86 emulation |
	 |               |
	 |               |
	 |               |
	 \ O             |
	  \______________|

	
homepage: http://libemu.mwcollect.org


building from svn:
  autoreconf -v -i
  ./configure --prefix=/opt/libemu; make install

building from tarball:
  tar xvfz libemu-VERSION.tar.gz
  cd libemu-VERSION
  ./configure --prefix=/opt/libemu; make install


specific configure options:

--enable-debug           enable debug code generation [default=yes]
	debug messages, instruction strings, nothing one wants to miss, even if it takes a lot of cpu cycles.

--enable-python-bindings enable build of the python bindings


Dependencies:
libdasm	https://github.com/jtpereyda/libdasm/
	wget https://github.com/jtpereyda/libdasm/archive/v1.6.tar.gz -O libdasm-1.6.tar.gz
	tar xzvf libdasm-1.6.tar.gz
	cd libdasm-1.6
	autoreconf -v -i
        make CFLAGS="$CFLAGS -fPIC" PREFIX=/opt/libdasm
        make


